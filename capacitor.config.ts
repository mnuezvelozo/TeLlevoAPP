import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aplicacion.tellevoapp',
  appName: 'aplicacion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
